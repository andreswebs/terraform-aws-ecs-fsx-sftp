#!/usr/bin/env bash

yum update -y

# shellcheck disable=SC2154
echo ECS_CLUSTER="${cluster_name}" >> /etc/ecs/ecs.config

# shellcheck disable=SC2154
${fsx_config_command}

# shellcheck disable=SC2154
${fsx_get_creds}

# shellcheck disable=SC2154
${fsx_mount_command}
